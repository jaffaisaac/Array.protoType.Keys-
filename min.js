// Array.prototype.keys()  this return s keys of each index in an array
const name =['joy','may','ben','bob']

const keysOnly = name.keys()

// using for-of loop 
for(keys of keysOnly){
    console.log(keys)
}